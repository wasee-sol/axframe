import * as React from "react";
import styled from "@emotion/styled";

interface Props {}

function PageInbox(props: Props) {
  return <PageInboxContainer>PageInbox</PageInboxContainer>;
}

const PageInboxContainer = styled.div``;

export default PageInbox;
