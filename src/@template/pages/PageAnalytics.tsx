import * as React from "react";
import styled from "@emotion/styled";

interface Props {}

function PageAnalytics(props: Props) {
  return <PageAnalyticsContainer>PageAnalytics</PageAnalyticsContainer>;
}

const PageAnalyticsContainer = styled.div``;

export default PageAnalytics;
