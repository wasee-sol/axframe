import * as React from "react";
import styled from "@emotion/styled";

interface Props {}

function UserInfo(props: Props) {
  return <UserInfoContainer>UserInfo</UserInfoContainer>;
}

const UserInfoContainer = styled.div``;

export default UserInfo;
